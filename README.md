# loopforge-bookmark-manager
A simple bookmark manager app. Users can save URLs with a title and optional tags. Bookmarks are displayed in a card grid sorted by most recent. Users can filter by tag and search by title. One table: bookmarks (id, url, title, tags text array, created_at). Seed with 5 sample bookmarks. Next.js frontend, Supabase backend, Tailwind CSS styling.
